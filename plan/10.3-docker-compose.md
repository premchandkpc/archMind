# 10.3 — Docker Compose

**Status:** Pending

---

## What this chunk does

7 services with health checks. One command: `docker compose up`

## Services

| Service | Port | Purpose |
|---------|------|---------|
| API | 8000 | FastAPI backend |
| Worker | — | Ingestion pipeline worker |
| PostgreSQL | 5432 | Metadata DB |
| Qdrant | 6333 | Vector DB |
| MinIO | 9000 | Object Storage |
| Neo4j | 7687 | Knowledge Graph |
| Redis | 6379 | Cache + Event Bus |

## Files to create

### docker-compose.yml

```yaml
services:
  api:
    build: .
    ports: ["8000:8000"]
    depends_on:
      postgres: {condition: service_healthy}
      qdrant: {condition: service_healthy}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
  
  worker:
    build: .
    command: ["python", "-m", "civilmind.events.workers"]
    depends_on:
      redis: {condition: service_healthy}
  
  postgres:
    image: postgres:16
    healthcheck:
      test: ["CMD-SHELL", "pg_isready"]
  
  qdrant:
    image: qdrant/qdrant:latest
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6333/healthz"]
  
  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
  
  neo4j:
    image: neo4j:5
  
  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
```
