# 2.5 — Calculator + CodeSearch Tools

**Status:** Done

---

## What this chunk does

Safe math evaluation and building code/regulation lookup.

## Files to create

### civilmind/tools/calculator.py

```python
class CalculatorTool(BaseTool):
    name = "calculator"
    description = "Perform mathematical calculations"
    category = "calculation"
    
    async def execute(self, expression: str) -> ToolResult:
        """Safe math eval (no eval(), use ast.literal_eval or a math parser)."""
```

### civilmind/tools/code_search.py

```python
class CodeSearchTool(BaseTool):
    name = "code_search"
    description = "Search building codes and regulations"
    category = "knowledge"
    
    async def execute(self, code: str, section: str | None = None) -> ToolResult:
        """Search regulation database."""
```

## Security

- Calculator: No `eval()`, use AST parser or sympy
- CodeSearch: Read-only, no user input in queries
