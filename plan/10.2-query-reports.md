# 10.2 — Query + Reports

**Status:** Pending

---

## What this chunk does

POST /api/query runs the full LangGraph workflow.
SSE streaming for long queries.
Report retrieval and download.

## Files to create

### civilmind/api/routes/query.py

| Method | Path | Description |
|--------|------|-------------|
| POST | /api/query | Run query (returns answer) |
| POST | /api/query/stream | SSE streaming |

### civilmind/api/routes/reports.py

| Method | Path | Description |
|--------|------|-------------|
| GET | /api/reports | List reports |
| GET | /api/reports/{id} | Get report |
| GET | /api/reports/{id}/download | Download PDF |

## Query request

```json
{
  "project_id": "uuid",
  "question": "Estimate total concrete required",
  "document_ids": ["optional", "filter"]
}
```

## Query response

```json
{
  "answer": "Based on the provided documents...",
  "sources": [
    {"chunk_id": "...", "content": "...", "score": 0.92, "page": 5}
  ],
  "reasoning_chain": [
    {"node": "planner", "action": "Identified estimation task"},
    {"node": "retriever", "action": "Found 15 relevant chunks"},
    {"node": "estimator", "action": "Calculated 450 cum concrete"}
  ],
  "confidence": 0.85,
  "cost_usd": 0.02,
  "latency_ms": 4200
}
```
