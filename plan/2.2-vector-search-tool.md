# 2.2 — VectorSearch Tool

**Status:** Pending

---

## What this chunk does

Semantic + keyword search via hybrid retriever.
Agents call this tool, never Qdrant directly.

## Files to create

### civilmind/tools/vector_search.py

```python
class VectorSearchTool(BaseTool):
    name = "vector_search"
    description = "Search project documents using semantic similarity"
    category = "retrieval"
    
    async def execute(self, query: str, project_id: str, top_k: int = 10) -> ToolResult:
        """Hybrid search: BM25 + Vector + RRF + Rerank."""
```

## Integration

- Uses HybridRetriever from Phase 4
- Returns ToolResult with chunks + scores + latency
- Logs every call for observability

## Failure scenarios

| Scenario | Behavior |
|----------|----------|
| Qdrant down | ToolResult(success=False, error="Vector DB unavailable") |
| Empty results | ToolResult(success=True, data=[]) |
| Timeout | ToolResult(success=False, error="Timeout after 5s") |
